# 使用、分享与扩展

## 发布与分享

网页包含118种元素、10个分子实例和7个离子化合物实例。GitHub 仓库保存源码，GitHub Pages 提供可以直接使用的网页。应分享 Pages 部署生成的网站链接；仓库链接适合查看源码或下载项目。

首次上传：将项目文件上传到自己的仓库，包含隐藏的 .github/workflows/pages.yml。在 Settings → Pages 选择 GitHub Actions，再在 Actions 中运行 Deploy Pages。后续推送 main 分支会先校验再发布。

网站发布后独立运行，不需要保持上传者登录。临时使用另一个账号时，可以在浏览器无痕窗口登录目标账号；命令行使用单独的 GH_CONFIG_DIR 完成授权和上传，结束后清理临时认证。不要把认证文件或令牌提交进仓库。

## 网页压缩包

静态文件位于 dist：index.html、styles.css、elements.js、electron-model.js、data.js、i18n.js、app.js。七个文件保持同一目录，可直接打开 index.html，或整体上传到静态主机。element-explorer-web.zip 仅包含这些网页文件。

## 维护职责

| 文件 | 内容 |
|---|---|
| sources/pubchem-periodic-table.json | 118元素原始数据快照 |
| scripts/build-elements.cjs | 数据转换、中文名、来源与人工修正 |
| dist/elements.js | 生成的元素参考数据 |
| dist/electron-model.js | 排布展开、电子计数、角向形状 |
| dist/data.js | 17个化合物条目及几何结构 |
| dist/app.js | 界面联动、周期表和绘图 |
| dist/i18n.js | 语言偏好与英文内容 |
| dist/index.html / styles.css | 页面文字、结构和样式 |
| verify.cjs | 数据和模型的一致性检查 |

## 扩展化合物

COMPOUNDS 数组没有固定数量上限，可逐步增加条目。当前绘图器支持单中心、最多四个外围原子的路易斯图；乙醇、苯等多中心或环状结构需要先扩展布局算法。离子图显示种类与电荷关系，不是完整晶胞。

新增条目应包含可靠来源、元素组成、原子坐标、键索引与键级、孤电子对、几何说明和教学步骤。先验证电子总数、键连接及空间结构，再进行桌面和手机预览。几百条数据是否流畅需实际测试，不能仅依据数组容量保证。

## 数据导入

当前没有用户上传分子数据的入口。后续可增加经过字段校验的 JSON 或 SDF/MOL 导入。XYZ 只包含坐标，不能直接提供完整成键信息；导入坐标也不会自动生成可靠的性质和教学解释。

## 信息呈现

标题说明内容，按钮说明动作；数值旁边标注单位和来源；缺失数据写“未提供”；区分参考事实、计算计数和图形示意。优先增加化合物分类搜索、两元素对比，并按需展开解释，避免继续堆积主界面按钮。
